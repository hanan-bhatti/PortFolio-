# Display Skills and Experience on Homepage

## Goal
Fetch skills and experience data from the database and render them dynamically on the public homepage ([app/(public)/page.tsx](file:///home/hanan-bhatti/Downloads/portfolio/app/(public)/page.tsx)) using a responsive Brutalist style.

## Tasks
- [x] Task 1: Create the `SkillsSection` component in `components/ui/` to display categories, icons, and progress bars.
- [x] Task 2: Create the `ExperienceSection` component in `components/ui/` to show a vertical timeline of professional roles.
- [x] Task 3: Fetch skills and experiences in `app/(public)/page.tsx` and pass them to the new components.
- [x] Task 4: Run validation tools (`npx tsc --noEmit && npm run lint`) to check for errors.

## Done When
- [x] Skills and experiences from the database render correctly on the homepage.
- [x] Responsive design functions on mobile, tablet, and desktop viewports.
- [x] Static build compiles successfully without errors.
